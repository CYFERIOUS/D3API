#!/usr/bin/env node
var app = require('../app');
var port = process.env.PORT || 3000;

app.set('port', port);

app.listen(port, function() {
  console.log('API-D3 is available at http://localhost:' + port);
});
